## 1. Каркас проекта

- [ ] 1.1 Создать `expense_tracker.py` с argparse: подкоманды `add`, `list`, `summary` и их аргументы
- [ ] 1.2 Реализовать загрузку/сохранение JSON-файла (атомарная запись через tempfile + rename)

## 2. Команда add

- [ ] 2.1 Валидация суммы (положительное число), категории (из списка), даты (YYYY-MM-DD, существующая)
- [ ] 2.2 Создание записи с UUID4 и сохранение в JSON

## 3. Команда list

- [ ] 3.1 Вывод всех записей в табличном формате, сортировка по дате (новые сверху)
- [ ] 3.2 Фильтрация по флагам `--from` / `--to` (включительно)
- [ ] 3.3 Фильтрация по флагу `--category` (с валидацией)

## 4. Команда summary

- [ ] 4.1 Агрегация по категориям: сумма + количество, итоговая строка
- [ ] 4.2 Поддержка фильтров `--from` / `--to` / `--category`, скрытие пустых категорий

## 5. Тесты

- [ ] 5.1 Тесты валидации (неверная категория, сумма, дата)
- [ ] 5.2 Тесты add/list/summary через subprocess или вызов main()
